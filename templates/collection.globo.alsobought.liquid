{% comment %}Do not edit this file{% endcomment %}
{%- layout none -%}{%- if collection.all_products_count > 0 -%}{%- assign limit = 80 -%}{%- assign count = 0 -%}{%- paginate collection.products by 80 -%}[{%- for product in collection.products -%}{%- if product.available -%}{%- unless count == 0 -%},{%- endunless -%}
{
  "id":{{- product.id -}},
  "available": {{- product.available -}},
  "title":{{- product.title |json -}},
  "handle":{{- product.handle |json -}},
  "images":{{- product.images | json -}},
  "tags":{{- product.tags |json -}},
  "collections": {{- product.collections | json -}},
  "type":{{- product.type |json -}},
  "variants":{{- product.variants |json -}},
  "vendor":{{- product.vendor |json -}},
  "price":{{- product.price |json -}},
  "compare_at_price":{{- product.compare_at_price |json -}},
  "stamped_badge": {{ product.metafields.stamped.badge |json }}
}
{%- assign count = count | plus: 1 -%}{%- if count >= 80 -%}{%- break -%}{%- endif -%}{%- endif -%}{%- endfor -%}]{%- endpaginate -%}{%- else -%}[]{%- endif -%}
